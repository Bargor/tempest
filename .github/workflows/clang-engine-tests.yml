name: clang-engine-tests
on: [push]

jobs:
  build:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        cfg:
        - {build_type: Debug, engine: Vk, extra_flags: "-stdlib=libc++"}
        - {build_type: Release, engine: Vk, extra_flags: "-stdlib=libc++"}
        - {build_type: Debug, engine: GL, extra_flags: "-stdlib=libc++"}
        - {build_type: Release, engine: GL, extra_flags: "-stdlib=libc++"}
        - {build_type: Debug, engine: GL, extra_flags: "-stdlib=libc++ -fsanitize=thread -O1"}
        - {build_type: Debug, engine: GL, extra_flags: "-stdlib=libc++ -fsanitize=address -O1 -fno-omit-frame-pointer -fno-optimize-sibling-calls"}
        - {build_type: Debug, engine: GL, extra_flags: "-stdlib=libc++ -fsanitize=undefined -fno-omit-frame-pointer"}
    steps:
    - name: checkout
      uses: actions/checkout@v1
      with:
        submodules: recursive
    - name: install
      run: sudo apt-get install -y libgl1-mesa-dev libxinerama-dev libxcursor-dev xorg-dev libc++-18-dev libc++abi-18-dev libwayland-bin libwayland-dev libxkbcommon-dev
    - name: build GLEW    
      if: ${{ matrix.cfg.engine == 'GL'}}
      env:
        CC: clang-18
        CXX: clang++-18
      run: |
        cd libs/glew/build;
        cmake ../build/cmake/.;
        sudo make -j2
        sudo make install;
        cd ../../../;
    - name: install vulkan
      if: ${{ matrix.cfg.engine == 'Vk' }}
      run: |
        wget -qO - http://packages.lunarg.com/lunarg-signing-key-pub.asc | sudo apt-key add -;
        sudo wget -qO /etc/apt/sources.list.d/lunarg-vulkan-1.3.296-noble.list http://packages.lunarg.com/vulkan/1.3.296/lunarg-vulkan-1.3.296-noble.list;
        sudo apt update;
        sudo apt install vulkan-sdk;
    - name: config CMake
      env:
        CC: clang-18
        CXX: clang++-18
        BUILD_TYPE: ${{ matrix.cfg.build_type }}
        ENGINE: ${{ matrix.cfg.engine }}
        EXTRA_FLAGS: ${{ matrix.cfg.extra_flags }}
      run: | 
        mkdir build
        cd build
        cmake .. -DCMAKE_BUILD_TYPE=$BUILD_TYPE -DCMAKE_CXX_FLAGS="${EXTRA_FLAGS}" -DTEMPEST_BUILD_ALL=ON -DENGINE="$ENGINE" -D CMAKE_FIND_DEBUG_MODE=ON
        make -j2
    - name: run core tests
      run: ./build/exe/core_test
    - name: run queue tests
      if: ${{ matrix.cfg.build_type == 'Release' }}
      run: ./build/exe/queue_test
